# Copy this file to .env and fill in real values.
# .env is gitignored - never commit real credentials.

# UI under test (base URL used by Playwright)
UI_BASE_URL=https://horizon-plus.dfp8hwwhcxnpq.amplifyapp.com

# Login credentials (populated when available; tests must fail fast if missing)
LOGIN_EMAIL=
LOGIN_PASSWORD=
