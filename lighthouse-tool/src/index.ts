/**
 * Placeholder module entry point.
 * Pipeline stages (validate-url, run-lighthouse, parse-results, rule-engine,
 * gates, report) are added in later phases; this keeps the TS project valid
 * so `tsc --noEmit` verification works from Phase 0 onward.
 */
export { };
